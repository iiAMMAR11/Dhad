# المساهمة

مرحبًا بأي إصلاح أو إضافة تجعل Dhad أوضح وأسهل للتطبيق.

## قبل إرسال Pull Request

- اجعل التغيير محدودًا بمشكلة واحدة.
- استخدم التوكنز الدلالية وخصائص CSS المنطقية.
- لا تضف منطق منتج أو أسرارًا أو ملفات خطوط.
- اختبر العربية وRTL والوضعين الداكن والفاتح.
- اختبر لوحة المفاتيح والوصول والجوال والنص الطويل.
- اجعل كل تحكم ظاهر يعمل فعلًا.

شغّل التحقق قبل الإرسال:

```bash
python3 scripts/generate_tokens.py --check
node --test tests/*.test.mjs
python3 scripts/build_releases.py
python3 scripts/verify_public_release.py
```

للمشكلات الصغيرة افتح Issue، وللتغييرات الجاهزة افتح Pull Request مع وصف مختصر للنتيجة.
